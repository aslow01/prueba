function puedeManejar(edad) {
  if(edad >= 18) {
           console.log( true );
  } 
  console.log( false );
}

puedeManejar(20);


function tienenElMismoCaracter(str1, str2){
return str1.length === str2.length
}
